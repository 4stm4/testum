# Быстрый старт: Система обновлений

## Как проверить обновления

1. Откройте страницу **Settings** (⚙️ Настройки)
2. Прокрутите вниз до раздела **"🔄 Application Updates"**
3. Нажмите кнопку **"🔍 Check for Updates"**

## Что вы увидите

### Если обновление доступно:
- ✅ Текущая версия (например, v0.1.0)
- ✅ Доступная версия (например, v0.2.0)
- ✅ Описание изменений (Release Notes)
- ✅ Кнопка **"⬇️ Install Update"** станет активной

### Если обновлений нет:
- ✅ Сообщение "You are up to date!"
- ℹ️ Информация о текущей версии

## Как установить обновление

1. Нажмите **"⬇️ Install Update"**
2. Подтвердите действие в диалоговом окне
3. Дождитесь завершения загрузки
4. **Перезапустите приложение:**
   ```bash
   docker-compose restart
   ```
   или
   ```bash
   docker-compose up -d --build
   ```

## Важные замечания

⚠️ **Перед обновлением:**
- Убедитесь, что все изменения закоммичены
- Прочитайте Release Notes
- Создайте резервную копию данных

⚠️ **После обновления:**
- **Обязательно** перезапустите Docker контейнер
- Проверьте работоспособность приложения
- Проверьте логи: `docker-compose logs -f app`

## Возможные проблемы

### "Cannot update: uncommitted changes"

**Решение:**
```bash
# Проверить что изменилось
git status

# Закоммитить изменения
git add .
git commit -m "Мои изменения"

# Или спрятать изменения
git stash

# Попробовать обновление снова
```

### Обновление не применилось

**Решение:**
- Обязательно перезапустите контейнер после обновления
- Используйте `docker-compose up -d --build` если изменился `requirements.txt`

### Как откатить обновление

Если что-то пошло не так:

```bash
# Посмотреть историю коммитов
git log --oneline

# Откатиться на предыдущую версию
git checkout <commit-hash>
docker-compose restart
```

## API доступ

Можно также использовать API напрямую:

**Проверить обновления:**
```bash
curl http://localhost:8000/api/updates/check
```

**Установить обновление:**
```bash
curl -X POST http://localhost:8000/api/updates/perform \
  -H "Content-Type: application/json" \
  -d '{}'
```

## Дополнительная информация

Подробная документация: [UPDATES.md](UPDATES.md)
